import { AppColor } from '../constants';

const light = {
  ...AppColor,
  // add light theme color here
};

const dark = {
  ...AppColor,
  // add dark theme color here
};

export const colors = {
  light,
  dark,
};
